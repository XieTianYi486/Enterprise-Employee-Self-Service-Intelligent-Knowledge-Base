# ============================================================
# 企业员工自助服务系统 - Python 依赖
# 原则：全部 pip 安装，无需 Docker，无需编译大型库
# ============================================================

# --- Web 框架 ---
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20
aiofiles==24.1.0

# --- 数据库 ---
# SQLAlchemy 同步模式（与旧项目异步写法区分开）
sqlalchemy==2.0.36
pymysql==1.1.1          # MySQL 备用（当前用 SQLite）
cryptography==44.0.0    # SQLAlchemy 连接加密

# --- 向量数据库 ---
chromadb==0.5.23
# ChromaDB 自带 sqlite3 持久化，无需额外安装

# --- 关键词检索 ---
rank-bm25==0.2.2

# --- 阿里云百炼 AI ---
dashscope==1.20.11
openai==1.58.1           # 百炼 OpenAI 兼容模式客户端（流式 LLM 调用）

# --- 文档解析 ---
pymupdf==1.25.1         # PDF 解析（纯 Python，无需系统依赖）
python-docx==1.1.2      # Word 文档解析
openpyxl==3.1.5         # Excel 文档解析
markdown==3.7           # Markdown 解析

# --- 认证与安全 ---
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1

# --- 缓存 ---
cachetools==5.5.0
diskcache==5.6.3

# --- 图处理（知识图谱阶段用）---
networkx==3.4.2

# --- 工具库 ---
pydantic==2.10.4
pydantic-settings==2.7.0
python-dateutil==2.9.0
loguru==0.7.3           # 结构化日志（替代旧项目的 structlog）
httpx==0.28.1           # HTTP 客户端（百炼 API 调用 + 测试）

# --- 开发与测试 ---
pytest==8.3.4
pytest-asyncio==0.25.0
httpx==0.28.1
