---
description: 重新构建前端代码（Vue 3 + Vite）
---

请重新构建企业员工自助服务系统的前端代码：

```bash
cd "E:\Claude_Project_third\Enterprise Employee Self-Service Intelligent Knowledge Base\frontend" && npm run build
```

构建完成后，`frontend/dist/` 目录中的文件就是最新的打包结果。

如果构建失败，检查 TypeScript 编译错误并尝试修复。

注意：本项目后端是 Python FastAPI，修改后端代码无需重新构建，只需重启后端服务即可。
